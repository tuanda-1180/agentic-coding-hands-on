import ProfileScreen from "../components/profile/profile-screen";

// "Profile bản thân" — the signed-in user's personal profile page.
export default function ProfilePage() {
  return <ProfileScreen />;
}
